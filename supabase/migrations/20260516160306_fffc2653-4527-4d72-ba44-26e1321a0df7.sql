ALTER FUNCTION public.replace_cross_batch_clearings_for_run(uuid, jsonb, text)
  SECURITY DEFINER;

ALTER FUNCTION public.replace_cross_batch_clearings_for_run(uuid, jsonb, text)
  SET search_path = public;